services:
  - type: web
    name: mednexus-ai
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: ENV
        value: production
      - key: LLM_ENABLED
        value: "false"
      - key: CORS_ORIGINS
        value: "*"
