services:
  mednexus:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - mednexus_data:/app/data
      - mednexus_models:/app/ml_artifacts
    restart: unless-stopped
volumes:
  mednexus_data:
  mednexus_models:
